# legacy/ — retired docs (do not follow)

These files are **superseded and kept only for history**. They contain stale, contradictory
guidance. Do not brief from them.

- `PROJECT-DESIGN-RULES.md` — the older design contract. Replaced by `DESIGN.md` (the
  token/component/template system, incl. §6 the `.pd-*` detail-page recipe) + `Aura-design.md`
  (the effort bar). Its stale claims (a type filter on `projects.html`, `ffmpeg`/`convert`
  media tooling) are wrong; current truth lives in `CLAUDE.md` and `DESIGN.md`.
- `DEPLOY.md` — one-time GitHub/Vercel bootstrap. Already done (the site is live on Vercel).
  The only still-relevant note (the rivera.au DNS target) is now in `PROGRESS.md` → Open threads.

The live doc set is: `CLAUDE.md` · `PROGRESS.md` · `DESIGN.md` · `Aura-design.md`.
