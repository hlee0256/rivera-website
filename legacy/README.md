# legacy/ — retired docs (do not follow)

These files are **superseded and kept only for history**. They contain stale, contradictory
guidance. Do not brief from them.

- `PROJECT-DESIGN-RULES.md` — the older design contract. Replaced by `Aura-design.md` (Part I
  the effort bar + Part II the token/component/template system, incl. Part II §6 the `.pd-*`
  detail-page recipe). Its stale claims (a type filter on `projects.html`, `ffmpeg`/`convert`
  media tooling) are wrong; current truth lives in `CLAUDE.md` and `Aura-design.md`.
- `DEPLOY.md` — one-time GitHub/Vercel bootstrap. Already done (the site is live on Vercel).
  The only still-relevant note (the rivera.au DNS target) is now in `PROGRESS.md` → Open threads.

The live doc set is: `CLAUDE.md` · `PROGRESS.md` · `Aura-design.md` (plus the append-only
`CHANGELOG.md` and `TAKEAWAYS.md`). `DESIGN.md` was merged into `Aura-design.md` Part II.
